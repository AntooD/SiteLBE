{% extends 'base.html.twig' %}

{% block title %}Détails{% endblock %}

{% block body %}
</br>
</br>
</br>
    <div class="bg-light p-3">
        <h1>Détails : {{ product.title }}</h1>
    </div>

    <div class="row">
            <div class="col-4 mt-4">
                <div class="border border-dark rounded p-2 clearfix text-center">
                    <img class="img-fluid rounded" src="..\{{ product.image }}" alt="Image de {{ product.title }}">
                     <h2>
                        {{ product.title }}
                        -
                        {{ product.price }}
                        €
                    </h2>
                    <h3>
                     {{ product.description }}
                    </h3>
                    <hr>
                    <a href="{{ path('cart_add_this_product',{'id':product.id}) }}" class="btn btn-success float-right">
                        Ajouter au panier
                    </a>
                </div>

            </div>
    </div>
{% endblock %}